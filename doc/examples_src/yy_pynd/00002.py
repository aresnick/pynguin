def manyyy(n):
    import random
    for yy in range(n):
        color('random')
        c1 = color()
        color('random')
        c2 = color()
        goto('random')
        r = random.randrange(20, 100)
        yyc(r, c1, c2)
