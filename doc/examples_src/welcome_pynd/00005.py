def draw_square(size=100):
    '''draw a square with side
        length size'''
    for side in 1, 2, 3, 4:
        forward(size)
        left(90)


# Press "Test" if you make
# changes here to have the
# changes loaded in to the
# interpreter!
